#include <iostream>
#include <iomanip>
#include <set>
#include <cctype>
#include <algorithm>
#include <cmath>
using namespace std;


int main()
{
    int n;
    cin >>n;
    long long a;

    for (int i=0;i<n;i++){
        cin >> a;
        int b=sqrt(a);
        if (b*b==a && a>3){
            int j=1;
            bool test=true;
            int k=sqrt(b);
            while (j<k){
                if (k%j==0){
                    test=false;
                    break;
                }
                else {
                    j++;
                }
            }
            if (test==true){
                cout<<"YES"<<endl;
            }
            else {
                cout<<"NO"<<endl;
            }
        }
        else {
            cout <<"NO"<<endl;
        }
    }






    return 0;
}
